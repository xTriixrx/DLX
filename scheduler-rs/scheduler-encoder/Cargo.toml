[package]
name = "scheduler-encoder"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
scheduler-core = { path = "../scheduler-core" }
serde = { version = "1", features = ["derive"] }

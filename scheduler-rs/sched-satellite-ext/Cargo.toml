[package]
name = "sched-satellite-ext"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
abi_stable = "0.11"
scheduler-core = { path = "../scheduler-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
